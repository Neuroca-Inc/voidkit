[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[tool.maturin]
manifest-path = "../Cargo.toml"
# PyO3 0.22 requires the extension-module feature so Unix wheels do not link libpython.
features = ["extension-module"]
